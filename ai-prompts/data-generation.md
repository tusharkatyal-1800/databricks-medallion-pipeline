# AI Prompts — Data Generation (Phase 2)

## Prompt 1: Initial Data Generation Script

**PROMPT SENT:**
Generate a Python script `src/data_generation/generate_sample_data.py` that creates 
3 realistic e-commerce CSV files.
Requirements:
**customers.csv (10,000 rows):**
- customer_id: sequential INT starting from 1
- customer_name: realistic full names (use faker library)
- email: valid email format (firstname.lastname@domain.com), use realistic domains
- country: weighted distribution — 40% USA, 15% UK, 10% Germany, 10% India, 25% other countries
- signup_date: DATE between 2020-01-01 and 2025-12-31, weighted toward recent years
- customer_segment: 20% Premium, 50% Standard, 30% Basic
- lifetime_value: DECIMAL(10,2), correlated with segment:
  - Premium: $500-$5000
  - Standard: $100-$500
  - Basic: $10-$100
**Intentional quality issues in customers.csv:**
- Rows 9951-10000: Set email to NULL (50 rows)
- Rows 9941-9950: Duplicate customer_ids from rows 1-10 (10 rows)
**orders.csv (100,000 rows):**
- order_id: sequential INT starting from 1
- customer_id: random INT from valid customer_ids (1-10000)
- order_date: DATE between 2023-01-01 and 2025-12-31
- product_id: random INT from valid product_ids (1-500)
- quantity: INT between 1-10
- unit_price: DECIMAL(10,2), should match the product's price from products.csv
- total_amount: quantity * unit_price (calculate correctly)
- order_status: 60% Completed, 25% Pending, 15% Cancelled
- payment_date: NULL for Pending orders, order_date + 1-5 days for Completed, NULL for Cancelled
**Intentional quality issues in orders.csv:**
- Rows 99701-99800: Set customer_id to NULL (100 rows)
- Rows 99801-100000: Set product_id to NULL (200 rows)
- Rows 99651-99700: Set customer_id to random INT 90001-99000 (orphans, 50 rows)
- Rows 99621-99650: Set product_id to random INT 9001-9500 (orphans, 30 rows)
- Rows 99601-99620: Duplicate order_ids from rows 1-20 (20 rows)
**products.csv (500 rows):**
- product_id: sequential INT starting from 1
- product_name: realistic product names (e.g., "Wireless Bluetooth Headphones")
- category: from list [Electronics, Clothing, Home & Garden, Books, Sports, Beauty, Toys, Food & Beverage]
- price: DECIMAL(10,2) between $5.00 and $2000.00, correlated with category
- cost: DECIMAL(10,2) = price * random(0.3, 0.7) — represents wholesale cost
- stock_quantity: INT between 0-5000
- reorder_level: INT between 10-200
Script requirements:
- Use faker library for realistic names
- Use pandas for CSV generation
- Set random seed = 42 for reproducibility
- Add comments explaining each quality issue
- Print summary at end: total rows generated, total quality issues per type
- Save files to ./data/ directory


**AI RESPONSE SUMMARY:**
Created src/data_generation/generate_sample_data.py and ran it. Files are in ./data/.

What it generates

customers.csv — 10,000 rows (Faker names, firstname.lastname@domain, weighted country/segment/signup year, LTV by segment)
products.csv — 500 rows (8 categories, category-based prices, cost = price × 0.3–0.7)
orders.csv — 100,000 rows (unit_price from the product catalog, total_amount = quantity * unit_price, payment date only for Completed)
Planted issues (seed = 42)

50 NULL emails, 10 duplicate customer_ids (rows 9941–9950 copy ids 1–10)
100 NULL customer_id, 200 NULL product_id
50 orphan customers, 30 orphan products
20 duplicate order_ids
Total listed issues: 460 (type/domain plants were not in this prompt)
Re-run:

python -m pip install -r requirements.txt
python src/data_generation/generate_sample_data.py
Then upload `data/*.csv` to `/Volumes/ecommerce/medallion/data/raw/` for Bronze.

Documentation summary

Added src/data_generation/generate_sample_data.py (Faker + pandas, seed 42).
Writes data/customers.csv, data/orders.csv, data/products.csv with the specified schemas and planted defects.
Script logs writes, comments each quality issue, and prints a row/issue summary (460 listed issues).
Pinned pandas, numpy, and faker in requirements.txt.

**YOUR EVALUATION:**
The csv file generation scripts looks good as per the detailed prompt i gave , lets just run this script and then validate the data generated by them.


## Iteration 1 : Validation script generation to validate data generated .

**PROMPT SENT:**
I have just finished running the script for data generation. Here are the detailed summary i got from script output

========== Sample data generation summary ==========
customers.csv rows: 10000
orders.csv rows:     100000
products.csv rows:   500

Quality issues (customers):
  NULL emails:              50
  Duplicate customer_id rows (extra planted): 10
  Rows in duplicate key groups: 20 (10 distinct ids)

Quality issues (orders):
  NULL customer_id:        100
  NULL product_id:          200
  Orphan customer_id:       50
  Orphan product_id:         30
  Duplicate order_id rows (extra planted): 20
  Rows in duplicate key groups: 40

Total listed issue instances: 460
===================================================

Please help me write a simple script(src/data_generation/validate_generated_data.py) to check the following:
1. Load each CSV file using pandas
2. Check number of rows (10000, 100000, 500)
3. Count NULL values per column
4. Count duplicates in primary key columns
5. Check for orphan foreign keys
6. Print out a table showing expected quality issues vs actual

Expected quality issues:
- customers: 50 NULL emails, 10 duplicate customer_ids
- orders: 100 NULL customer_ids, 200 NULL product_ids, 50 orphan customer_ids, 30 orphan product_ids, 20 duplicate order_ids

**AI RESPONSE SUMMARY:**
Added src/data_generation/validate_generated_data.py. With the venv activated:

python src\data_generation\validate_generated_data.py
It loads the three CSVs, checks row counts, prints nulls per column, counts extra duplicate keys, and compares planted issues to expected. A local run PASSED.

How duplicate counts are read: “10 duplicate customer_ids” means 10 extra rows (duplicated(keep='first')). Those 10 keys appear twice, so 20 rows sit in duplicate groups. Same idea for orders: 20 extra rows, 40 group rows.

Orphans: planted counts match (50 / 30). A full FK check finds 157 missing customer_ids because rows 9941–9950 reused ids 1–10, so ids 9941–9950 are no longer in customers. The table scores the planted range 90001–99000; the extra orphans are printed as a note.

payment_date ~40k nulls is expected (Pending + Cancelled), not a planted completeness issue.

Documentation summary

Added src/data_generation/validate_generated_data.py (pandas, ./data/ CSVs).
Checks row counts, nulls, PK extras, orphans, and expected-vs-actual.
Validation passed: 50/10 customers; 100/200/50/30/20 orders (planted).
Extra customer orphans (157 total) are a side effect of duplicate customer_ids overwriting ids 9941–9950.

**YOUR EVALUATION:**
The data generated looks good and it got passed on all the validation checks , now we can proceed with the ETl pipeline implementation with this data files.

It is good that my data has various completeness issues which we will resolve in our ETL pipeline, some of these issues are planted and some naturally occured while generating data using our script.
If we see any issue in our data going further, we will come back and update our data generation script as per our requirement.

Also git ignore file is generated so we can ignore venv,etc files from being send to github

## Prompt 2: gitignore file generation

**PROMPT SENT:**
Create gitignore file to ignore venv and other env stuff from being send to github repo

**AI RESPONSE SUMMARY:**
Yes. Added a root .gitignore so Git skips local junk.

Ignored

Virtualenvs: .venv/, venv/, env/
Python cache/build: __pycache__/, *.pyc, .pytest_cache/, coverage
Generated CSVs: data/*.csv (recreate with seed 42)
Secrets: .env
Editor/OS: .idea/, .vscode/, .DS_Store
Still committed: source, requirements.txt, .cursorrules, and docs.

If you already staged the venv or CSVs:

git rm -r --cached .venv data
git add .gitignore
To include the CSVs in the evaluation repo, remove the data/*.csv line from .gitignore.

Documentation summary

Added .gitignore for venv, Python cache, generated data/*.csv, .env, and IDE files.
Source, requirements, and docs stay tracked; CSVs can be regenerated.

**YOUR EVALUATION:**

Git ignore file is generated so we can ignore venv,etc files from being send to github

we need to remove data directory from git ignore as we need to send data to git repo for submission.

## Iteration 1: gitignore file update

**PROMPT SENT:**
We need to send csv files to github for submission, pleas remove it from the gitignore

**AI RESPONSE SUMMARY:**
Removed data/*.csv from .gitignore. The generated files can go to GitHub.

**YOUR EVALUATION:**
Documentation summary

Stopped ignoring data/*.csv so sample CSVs can be committed for submission.
Venv, Python cache, and .env remain in .gitignore.
